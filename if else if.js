
        var per = 38

        if(per>=80 && per <=100)
        {
            document.write("you are in marit")
        }

         else if(per>=60 && per <=80)
        {
            document.write("you are in 1st division")
        }

         else if(per>=45 && per <=60)
        {
            document.write("you are in 2nd divison")
        }

         else if(per>=33 && per <=45)
        {
            document.write("you are in 3nd divison")
        }

         else if(per< 33 )
        {
            document.write("you are fall.")
        }

        else
        {
            document.write("plase enter valid percent")
        }















   